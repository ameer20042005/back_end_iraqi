# خطة نقل function call إلى vLLM

## 1. هل Gemma 4 12B يدعم التفكير + استدعاء الأدوات معاً؟

نعم يقدر — Gemma 4 12B عنده الاثنين أصلاً:

### التفكير العميق

وضع تفكير مدمج قابل للتهيئة، خطوة بخطوة قبل الجواب.
[Google AI](https://ai.google.dev/gemma/docs/core/model_card_4) — تشغّله بتوكن `<|think|>` ببداية دور الموديل.
[Ollama](https://ollama.com/library/gemma4:12b)

- **قاعدة مهمة:** لا تحتفظ بمحتوى التفكير بالتاريخ — الأدوار السابقة يدخل منها الجواب النهائي فقط.
  [Ollama](https://ollama.com/library/gemma4:12b)
- **استثناء:** داخل نفس الدور إذا فيه استدعاء أدوات، لا تحذف الـ thoughts بين الاستدعاءات.
  [Google AI](https://ai.google.dev/gemma/docs/capabilities/thinking) — أغلب مشاكل الـ tool calling سببها كسر هاي القاعدة.

### الـ function calling

بروتوكول أدوات خاص بتوكنات مخصصة.
[vLLM](https://recipes.vllm.ai/Google/gemma-4-12B-it) — على vLLM:

```text
--enable-auto-tool-choice
--reasoning-parser gemma4
--tool-call-parser gemma4
```

مع `tool_chat_template_gemma4.jinja`. [vLLM](https://recipes.vllm.ai/Google/gemma-4-12B-it)

- استخدم القالب الرسمي، لا تكتب فورمات JSON يدوي بالـ system prompt. هذا سبب شائع للفوضى.

## 2. مشكلة: "يستدعي أداة حتى لما تسأله عن اسمه"

السبب غالباً مو الموديل — هاي أشياء تفحصها بالترتيب:

1. **tool_choice** مضبوط على `required` أو `any` بدل `auto`. افحصه أول شي، أغلب الحالات هنا.
2. **وصف الأداة فضفاض:** مثلاً "search products" → الموديل يشوفها تناسب كل شي.
   خلّيها: "ابحث بكتالوج المنتجات. استخدمها فقط عند ذكر منتج أو سعر أو توفر. لا تستخدمها لأسئلة عن البوت نفسه أو التحية."
3. **عدد الأدوات:** فوق ~6-8 أدوات معروضة دفعة وحدة يزيد الخلط. مرّر بس الأدوات المتعلقة بالمرحلة الحالية من المحادثة.
4. **system prompt:** أضف صراحة — "أسئلة الهوية والتحية والشكر: جاوب مباشرة بدون أدوات."
5. **قوالب مضادة (few-shot):** 3-4 أمثلة أدوارها جواب نصي بدون أداة. هذا أقوى تأثير من أي تعليمة نصية.

### لو ضل يفشل بعد كل هذا

- شغّل الـ thinking mode — يقلل الاستدعاء الانعكاسي لأنه يفكر "هل هذا يحتاج أداة؟" قبل ما يقرر.
- أو مصنّف نية صغير قبل الموديل (حتى regex أو موديل صغير): إذا النية chitchat/identity → مرّر الطلب بدون أدوات أصلاً. أرخص وأضمن من محاولة إقناع الـ 12B.

> **التوصية النهائية:** الخيار الأخير هو اللي أنصح بيه بالإنتاج — الاعتماد على انضباط موديل 12B بالتمييز هو نقطة هشاشة، والـ router يحلها نهائياً.

## 3. ملاحظات حول البحث في المبيعات

- الاستدعاء الحالي **لا يبحث** بل يجب يطلّب كل البيانات في المبيعات.
- أما **gini bot** فهو يبحث فعلاً.
- أما **الصوتي** فهو في انتظار الحالات.

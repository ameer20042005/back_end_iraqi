# متطلبات GPU — **صارت شبه فارغة عمداً**: محرك التوليد الآن خادم vLLM منفصل
# (يثبّته start.sh تلقائياً — نسخة nightly بدعم Gemma4، تجيب معها torch
# المتوافق — عند أول إقلاع على Ubuntu 22.04 خام، انظر RUNPOD_DEPLOY.md)،
# والباك اند عميل HTTP رفيع فقط (httpx بـ requirements.txt).
#
# آلية transformers.generate() + micro-batching اليدوي القديمة حُذفت بالكامل
# من app/engine.py، ومعها peft (دمج LoRA صار بالنوتبوك حصراً — الموديل
# المرفوع مدموج جاهز) وbitsandbytes (كانت للتكميم، غير مستخدمة).
#
# transformers تبقى مطلوبة فقط لميزة تحويل الصوت لنص (Whisper يعمل داخل
# عملية FastAPI — app/features/order_intake/transcribe.py، ويحتاج ffmpeg
# أيضاً: يثبّته start.sh تلقائياً على Ubuntu الخام لو ناقص).
#
# ⚠️ كل الإصدارات أدناه مثبَّتة (==) بالضبط، لا حدود دنيا/عليا (>=/<) —
# لا تفتحها مرة ثانية. السبب (عطل حقيقي وقع 2026-07-26): سقف مفتوح خلّى
# pip يسحب أحدث نسخة عند كل إقلاع (start.sh يشغّل pip install بكل مرة)،
# فجاء transformers 5.x وكسر الخدمة كاملة — كل الردود صارت فارغة/جملة
# اعتذار ثابتة. السبب أن 5.x شدّد تحميل المُرمِّز فما عاد يبنيه ضمنياً من
# tokenizer_config.json وحده، فصار vocab_size=5 (رموز خاصة فقط) وكل نص
# عربي يتحوّل لـ <unk> قبل ما يوصل الموديل. الحل النهائي كان رفع
# tokenizer.json للمستودع، والتثبيت الدقيق هنا حتى ما تتكرر ترقية صامتة
# تكسر الإنتاج بلا أي تغيير بالكود. لترقية عمدية: بدّل الرقم بعد اختبار
# محلي كامل (فحص المُرمِّز أدناه بـ RUNPOD_DEPLOY.md)، لا تفتح السقف.
#
# ملاحظة توافق: vllm 0.26.0 يشترط transformers>=5.5.3 (والإصدار 4.x ما
# يعرف معمارية Gemma4UnifiedForConditionalGeneration أصلاً) — الإصدار
# المثبَّت أدناه داخل هذا المدى، ومُختبَر توافقه مع بقية الحزم هنا فعلياً
# (pip install --dry-run بلا أي تعارض).
transformers==5.14.1

# مكتبة الترميز الفعلية خلف transformers — مثبَّتة هي كمان بإصدار دقيق
# لأن العطل أعلاه كان بمستوى المُرمِّز، وترقية كبرى هنا تكسر تحميل
# المفردات بنفس الشكل. 0.22.2 هو أعلى إصدار يقبله transformers==5.14.1
# (يشترط tokenizers<=0.23.0,>=0.22.0 ولا يوجد 0.23.0 مستقر على PyPI).
tokenizers==0.22.2

# فك ترميز الصورة المرفوعة (app/features/order_intake/vision.py) — التحويل
# لـ data URI يصير بـ app/engine.py والاستدلال البصري عند خادم vLLM.
Pillow==12.3.0

# تحويل نص عراقي لصوت (app/features/voice_followup/tts.py) — موديل
# ameer4wisam/Habibi-TTS-IRQ، نسخة عراقية من F5-TTS (voice cloning من مقطع
# مرجعي). soundfile يكتب مخرَج الموديل (numpy array) كملف WAV.
f5-tts==1.1.22
soundfile==0.14.0
huggingface_hub==1.27.0

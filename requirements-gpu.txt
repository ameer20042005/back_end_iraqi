# متطلبات RunPod فقط (GPU/Linux) — لا تُثبَّت محلياً على Windows بدون CUDA.
#
# ⚠️ يتطلب Pod بدرايفر NVIDIA يدعم CUDA 13 (وليس فقط 12.8). ثلاث محاولات
# لتثبيت إصدار vllm/torch أقدم متوافق مع CUDA 12.8 فشلت كلها:
#   - vllm>=0.6.0 (قيد مفتوح): يسحب torch==2.11.0 (يحتاج CUDA 13) — يفهم
#     Gemma 4 صح، لكن يفشل على درايفر 12.8 بـ "NVIDIA driver ... too old".
#   - vllm==0.10.1.1 (torch==2.7.1, CUDA 12.8): لا يدعم معمارية Gemma 4
#     أصلاً — "rope_scaling should have a 'rope_type' key".
#   - vllm==0.19.0 (torch==2.10.0, CUDA 12.8): عنده ملف gemma4_mm.py لكن
#     transformers المرافقة (4.57.6) لا تعرف model_type "gemma4" بعد.
# الخلاصة: Gemma 4 يحتاج فعلياً vllm>=0.25 (وبالتبعية torch>=2.11، CUDA 13).
# لا تراجع لإصدار أقدم بدون فحص التوافق مع Gemma4ForConditionalGeneration من
# جديد — اختر Pod عنده درايفر NVIDIA يدعم CUDA 13 بدل تثبيت إصدار أقدم هنا.
vllm>=0.6.0

# اختياري: تحميل الموديل بـ INT8 (quantization="bitsandbytes" في app/config.py)
bitsandbytes>=0.43

# AutoProcessor لبرومبتات الصور (app/engine.py: render_multimodal_prompt) +
# تحويل الصوت لنص (app/features/order_intake/transcribe.py) — موجودة أصلاً
# كتبعية لـ vllm عادةً، مثبَّتة هنا صراحةً للوضوح. تحتاج أيضاً ffmpeg (ثبَّتناه
# بالـ Dockerfile عبر apt-get — لو تشغّل عبر start.sh مباشرة على Pod تأكد إنه
# مثبَّت: `apt-get install -y ffmpeg`).
transformers>=4.44

# فك ترميز الصورة المرفوعة (app/features/order_intake/vision.py) — الوصف
# نفسه يصير عبر نفس محرك vLLM المستخدَم بباقي الميزات، بدون تحميل موديل ثانٍ.
Pillow>=10.0
